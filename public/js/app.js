angular
    .module('myApp', 
        ['ngRoute',
        'appRoutes', 
        'MainCtrl',
        'LogCtrl',
        'regUCtrl',
        'regVCtrl'
        ]).config(function($logProvider){
        $logProvider.debugEnabled(false);
    });